{
  "name": "oceanpoint",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ oceanpoint",
    "create-local": "graph create --node http://localhost:8020/ oceanpoint",
    "remove-local": "graph remove --node http://localhost:8020/ oceanpoint",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 oceanpoint"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.24.0",
    "@graphprotocol/graph-ts": "0.24.0"
  }
}
